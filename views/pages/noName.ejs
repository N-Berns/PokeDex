<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/header") %>
    <style>
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        .container {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #dc3545;
            margin-bottom: 1rem;
        }
        .btn {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
        }
        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>No Pokémon Found</h1>
        <p>Sorry, we couldn't find a Pokémon with that name. Please try again.</p>
        <form action="/search" method="GET">
            <button type="submit" class="btn">Go Back to Search</button>
        </form>
    </div>
    <%- include("../partials/footer") %>         
</body>
</html>
